work_dir: "workdir/agirobot_digital"

data:
  scene_type: "video"  # 'sceneflow' or 'video'
  height: 480
  width: 640
  rgb_path: "data/agirobot_digital/observations/digitaltwin_2/f2b5efec-f2be-4e2a-b7d2-a91d87215d19/video/head.mp4"
  fps: 15

generation:
  prompt_upsampler_ckpt: "/data2/yang_liu/Cosmos/checkpoints/Pixtral-12B"
  negative_prompt: "lowres, bad anatomy, bad hands, cropped, digital rendering, 3D rendering, computer rendering, virtual, worst quality"
  prompt:
    # agirobot_digitaltwin_2_upsampled: "The video depicts a virtual dining scene. A table is set with various items: a red apple, a red cup, a white mug, a black vase with flowers, and a small bowl with fruit. Two chairs are positioned around the table. A robotic arm enters the frame, picking up the red apple and placing it near the cup."
    agirobot_digitaltwin_2_warm: "warm atmosphere, at home, dining room, robotic arm, bright" 

  # VidToMe args. See details in "src/vidtome/patch.py, apply_patch()"
  local_merge_ratio: 0.9
  global_merge_ratio: 0.8
  frame_range: [0, -1, 2]

post_opt:
  apply_opt: True

seed: 123
base_config: plugin/VidToMe/configs/iclight_default.yaml