work_dir: "workdir/carla/iclight_carla_vidtome_opt_vid2nd_hardmsk_flow"

data:
  alpha: 0.75
  beta: 1e6  # simulate hard mask
  voxel_size: 

post_opt:
  apply_opt: True
  epochs_exposure: 0  # only second stage
  epochs: 105  # aligns with epochs of original setting
  lambda_dssim: 0.4

base_config: plugin/VidToMe/configs/carla/iclight_carla_vidtome.yaml