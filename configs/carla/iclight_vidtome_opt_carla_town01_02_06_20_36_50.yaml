work_dir: "workdir/carla"

data:
  scene_type: "carla"  # 'sceneflow' or ...
  height: 448
  width: 800
  data_dir: "data/carla/data_collect_town01_results"
  scene_name: "routes_town01_02_06_20_36_50"
  voxel_size: 2.0
  fps: 15

generation:
  prompt_upsampler_ckpt: "/data2/yang_liu/Cosmos/checkpoints/Pixtral-12B"
  negative_prompt: "lowres, bad anatomy, bad hands, cropped, digital rendering, 3D rendering, computer rendering, virtual, worst quality"
  prompt:
    town01_02_06_20_36_50_morning: "Street, in morning, modern cars" 

  # VidToMe args. See details in "src/vidtome/patch.py, apply_patch()"
  local_merge_ratio: 0.6
  global_merge_ratio: 0.5
  alpha_t: 0.05
  max_downsample: 4
  frame_range: [0, -1, 1] # start, end, interval. == [0, 32] == [32]

post_opt:
  apply_opt: True

seed: 123
base_config: plugin/VidToMe/configs/iclight_default.yaml